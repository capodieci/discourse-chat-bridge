*.sqlite
*.sqlite3
*.db
config.php
config.local.*
.env
*.pem
*.key
*.log

# Host specific inventory. Useful to us, but it is a reconnaissance sheet for
# anyone targeting the forum, so it stays out of the public repository.
docs/local/
